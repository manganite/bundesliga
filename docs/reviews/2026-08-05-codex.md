Audit findings
High — postponed fixtures can freeze at an obsolete kickoff.
[preMatch.mjs (line 147)](/home/thomas/dev/bundesliga/pipeline/src/preMatch.mjs:147) freezes an existing entry when either the current or previously stored kickoff has passed. If an August 8 fixture is postponed to August 20, the August 8 entry freezes permanently and never receives snapshots published before August 20. I reproduced this: the stored kickoff and ratings remained August 8/s1 after postponement, despite a newer s2 snapshot. The current tests do not cover rescheduling.

Low — release version is inconsistent with the lockfile.
[apps/public/package.json (line 4)](/home/thomas/dev/bundesliga/apps/public/package.json:4) declares 2.3.3, while [package-lock.json (line 29)](/home/thomas/dev/bundesliga/package-lock.json:29) still records 2.3.2. npm ci currently accepts this, but regenerating the lockfile produces unrelated drift and leaves release metadata inconsistent.

One deployment caveat: tag v2.3.3 still contains the old July 23 pre-match entries; for example [bl2/prematch.json (line 12)](/home/thomas/dev/bundesliga/data/seasons/2026/bl2/prematch.json:12). The newly merged pipeline should heal these during its next scheduled run, but the tagged/deployed data itself is not yet healed.
Validation completed:
All 60 tests passed.
Both production applications built successfully.
npm audit: 0 vulnerabilities.
Generated season data has 306 unique, matching fixtures per league, with no missing entries, team mismatches, or kickoff mismatches.
Working tree remains clean.