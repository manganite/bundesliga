// The surface under test. Bundled by vite (the app's own toolchain) so the tests
// exercise the same JSX the browser gets, not a re-implementation of it.
export { default as Relegation } from "../../src/components/Relegation.jsx";
export { default as TabelleUndPrognose } from "../../src/pages/TabelleUndPrognose.jsx";
export { default as App } from "../../src/App.jsx";
